// Deployment config — served next to index.html so it can be edited on the host
// without rebuilding. Point this at the backend, e.g. on Railway:
//   window.API_BASE_URL = 'https://your-service.up.railway.app';
// Leave empty when frontend and backend share an origin.
window.API_BASE_URL = 'https://liai.magizhchi.software';
